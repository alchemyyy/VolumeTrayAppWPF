<Window
    x:Class="VolumeTrayAppWPF.WPF.UpdateConfirmationWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
    xmlns:utils="clr-namespace:VolumeTrayAppWPF.WPF.Utils"
    xmlns:loc="clr-namespace:VolumeTrayAppWPF.Localization"
    utils:KeyboardFocusBehavior.EnableKeyboardOnlyFocus="True"
    utils:KeyboardFocusBehavior.FocusStyle="{StaticResource KeyboardFocusVisualStyle}"
    Title="{loc:Loc UpdateDialog_DefaultTitle}"
    Width="520"
    SizeToContent="Height"
    MinWidth="420"
    Background="{DynamicResource ThemeBackground}"
    Foreground="{DynamicResource ThemeForeground}"
    WindowStyle="None"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    ShowInTaskbar="False"
    TextElement.FontFamily="Segoe UI Variable, Segoe UI"
    TextOptions.TextFormattingMode="Ideal"
    TextOptions.TextRenderingMode="ClearType"
    UseLayoutRounding="True">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="32"
                            ResizeBorderThickness="0"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Border x:Name="RootBorder"
            Background="{DynamicResource ThemeBackground}"
            BorderBrush="{DynamicResource ThemeBorder}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="TitleText"
                           Grid.Column="0"
                           Foreground="{DynamicResource ThemeForeground}"
                           VerticalAlignment="Center"
                           Margin="16,0,0,0"
                           FontSize="13"/>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button x:Name="CloseButton"
                            Style="{StaticResource CaptionCloseButtonStyle}"
                            Click="Cancel_Click"
                            Content="&#xE8BB;"
                            ToolTip="{loc:Loc UpdateDialog_CaptionClose_Tooltip}"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Margin="28,8,28,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           x:Name="HeaderText"
                           Style="{StaticResource SectionHeaderStyle}"/>

                <TextBlock Grid.Row="1"
                           x:Name="DescriptionText"
                           Style="{StaticResource SettingDescriptionStyle}"
                           Text="{loc:Loc UpdateDialog_DefaultDescription}"
                           Margin="0,0,0,12"/>

                <Border Grid.Row="2"
                        Background="{DynamicResource ThemeControlBackground}"
                        BorderBrush="{DynamicResource ThemeBorder}"
                        BorderThickness="1"
                        CornerRadius="{DynamicResource CornerRadiusMedium}"
                        Padding="12,8">
                    <ScrollViewer x:Name="ChangelogScrollViewer"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <TextBlock x:Name="ChangelogText"
                                   Foreground="{DynamicResource ThemeForeground}"
                                   TextWrapping="Wrap"
                                   FontFamily="Consolas, Cascadia Mono, Segoe UI"
                                   FontSize="12"
                                   LineHeight="16"
                                   LineStackingStrategy="BlockLineHeight"/>
                    </ScrollViewer>
                </Border>

                <StackPanel Grid.Row="3"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,20,0,0">
                    <Button x:Name="CancelButton"
                            Style="{StaticResource SettingsButtonStyle}"
                            Content="{loc:Loc UpdateDialog_Cancel}"
                            Padding="20,8"
                            Margin="0,0,8,0"
                            Click="Cancel_Click"/>
                    <Button x:Name="InstallButton"
                            Style="{StaticResource SettingsButtonStyle}"
                            Content="{loc:Loc UpdateDialog_Install}"
                            Padding="20,8"
                            IsDefault="True"
                            Click="Install_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
